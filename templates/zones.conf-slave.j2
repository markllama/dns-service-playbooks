include "/etc/named/update.key" ;

zone {{ zone_name }} {
  type slave ; 
  masters { {% for ns in nameservers %} {%- if ns.name in zone_masters -%} {{ ns.address }} ; {% endif -%} {% endfor -%} } ;
  file "/var/named/dynamic/zone.db" ;
  allow-update-forwarding { any ; } ;
};
